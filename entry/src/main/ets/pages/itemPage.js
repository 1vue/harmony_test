
class item {
    constructor(name, price, img, discount = 0) {
        this.name = name;
        this.price = price;
        this.img = img;
        this.discount = discount;
    }
}
function itemCard(item) {
    .width("100%")
        .backgroundColor('#FFF')
        .borderRadius(20)
        .height(120)
        .padding(20);
}
function fillScreen() {
    
  
        .width('100%')
        .height('100%')
        .backgroundColor('#EFEFEF')
        .padding(20);
}
struct itemPage extends   {
    constructor(
    ) {
    }
    build() {
        .width('100%')
            .height('100%')
            .backgroundColor('#EFEFEF')
            .padding(20);
    }
}
//# sourceMappingURL=itemPage.js.map